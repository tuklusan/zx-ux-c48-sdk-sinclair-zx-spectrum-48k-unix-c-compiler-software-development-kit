# ailmzx48 model

This directory will contain the generated compact runtime model and its format/provenance documentation once the model architecture is frozen. No model format is frozen by this scaffold.
