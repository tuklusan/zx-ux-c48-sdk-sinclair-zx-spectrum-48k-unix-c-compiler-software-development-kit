# ============================================================================
# Copyright (c) 2026 Supratim Sanyal of SANYALnet Labs.
# Proprietary rights reserved except as expressly licensed herein.
#
# ZX-UX C48 SDK
# This file is governed by the SANYALnet Labs Non-Commercial License in the
# root LICENSE file. Non-Commercial use is permitted; Commercial Use and use
# for AI/ML model training are prohibited unless separately authorized.
#
# Attribution is required: "Based on original work by Supratim Sanyal of
# SANYALnet Labs." See LICENSE for full terms, warranty disclaimer, termination,
# patent, trademark, and governing-law provisions.
# ============================================================================
name: ailmzx48 maintenance

"on":
  push:
    branches: [main]
    paths:
      - ".github/ailmzx48-maintenance-request.txt"

permissions:
  contents: write

concurrency:
  group: ailmzx48-maintenance-main
  cancel-in-progress: false

jobs:
  maintain:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out current main
        uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Validate maintenance marker
        id: marker
        shell: bash
        run: |
          if test -f .github/ailmzx48-maintenance-request.txt; then
            echo "present=true" >> "$GITHUB_OUTPUT"
            git rm .github/ailmzx48-maintenance-request.txt
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "Maintenance marker is absent; marker-removal event is a clean no-op."
          fi

      - name: Test ailmzx48 context reference
        if: steps.marker.outputs.present == 'true'
        run: >-
          python -B usr/src/ailmzx48/evaluation/test_context_reference.py

      - name: Test ailmzx48 A48M reference
        if: steps.marker.outputs.present == 'true'
        run: >-
          python -B usr/src/ailmzx48/evaluation/test_a48m_reference.py

      - name: Compile ailmzx48 SDK artifact
        if: steps.marker.outputs.present == 'true'
        run: >-
          python -B compiler/c48.py
          usr/src/ailmzx48/ailmzx48.c
          -o usr/bin/ailmzx48/ailmzx48.c48b

      - name: Evaluate iterative exit criteria
        if: steps.marker.outputs.present == 'true'
        run: >-
          python -B usr/src/ailmzx48/evaluation/check_exit_criteria.py
          --write-status usr/src/ailmzx48/training/goal-status.json

      - name: Regenerate release manifest and apply quota optimization
        if: steps.marker.outputs.present == 'true'
        run: python -B usr/src/ailmzx48/tooling/update_manifest.py

      - name: Install focused GUI proof dependencies
        if: steps.marker.outputs.present == 'true'
        run: python -m pip install Pillow==12.3.0 python-xlib==0.33

      - name: Prove Mandel stress stays inside 15-second binary quota
        if: steps.marker.outputs.present == 'true'
        shell: bash
        run: >-
          python -B compiler/verify_graphics_demos.py
          --demo mandel
          --runner ubuntu-latest
          --time-quota 15
          --evidence-dir "$RUNNER_TEMP/c48-mandel-quota-proof"

      - name: Prove host-native Tk desktop after quota optimization
        if: steps.marker.outputs.present == 'true'
        shell: bash
        env:
          C48_GUI_COMMIT: ${{ github.sha }}
          C48_GUI_RUNNER: ubuntu-latest
        run: >-
          xvfb-run -a -s "-screen 0 1600x1200x24"
          python -B compiler/verify_gui_desktop.py
          --evidence-dir "$RUNNER_TEMP/c48-gui-quota-proof"

      - name: Verify complete repository
        if: steps.marker.outputs.present == 'true'
        shell: bash
        run: |
          python -B compiler/verify_release.py
          git diff --check

      - name: Commit and push maintenance checkpoint
        if: steps.marker.outputs.present == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email \
            "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Complete ailmzx48 maintenance checkpoint"
          git push origin HEAD:main
